const request = require('request');
const cheerio = require('cheerio');

function getInfromScorecard(url)
{
    console.log("from scorecards.js",url);
    // request(url,cb);
}

module.exports = {
    gifs:gitIntoFormScorecard,
};


